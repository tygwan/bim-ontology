services:
  api:
    build: .
    ports:
      - "${BIM_PORT:-8000}:8000"
    volumes:
      - ./references:/app/references:ro
      - ./data:/app/data
    environment:
      - LOG_LEVEL=info
    restart: unless-stopped
