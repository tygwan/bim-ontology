version: "3.8"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./references:/app/references:ro
      - ./data:/app/data
    environment:
      - LOG_LEVEL=info
    restart: unless-stopped
